name: Doğrula

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    if: ${{ github.repository == 'aaltebigep/data' }}
    steps:
      - uses: actions/checkout@v5
      - uses: nekowinston/setup-deno@main
      - run: deno task data:validate
